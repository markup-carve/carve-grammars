{
  "$schema": "https://raw.githubusercontent.com/martinring/tmlanguage/master/tmlanguage.json",
  "name": "carve",
  "scopeName": "source.carve",
  "fileTypes": [
    "crv"
  ],
  "aliases": [
    "crv"
  ],
  "patterns": [
    {
      "include": "#frontmatter"
    },
    {
      "include": "#fenced_code"
    },
    {
      "include": "#heading"
    },
    {
      "include": "#hr"
    },
    {
      "include": "#block_comment"
    },
    {
      "include": "#line_comment"
    },
    {
      "include": "#figure_group"
    },
    {
      "include": "#admonition"
    },
    {
      "include": "#block_comment_on_quote_marker_line"
    },
    {
      "include": "#blockquote"
    },
    {
      "include": "#abbreviation"
    },
    {
      "include": "#footnote_def"
    },
    {
      "include": "#link_ref_def"
    },
    {
      "include": "#caption"
    },
    {
      "include": "#definition_list"
    },
    {
      "include": "#table_row"
    },
    {
      "include": "#list_continuation"
    },
    {
      "include": "#multiline_cell"
    },
    {
      "include": "#callout_annotation"
    },
    {
      "include": "#task_item"
    },
    {
      "include": "#list_item"
    },
    {
      "include": "#numbered_item"
    },
    {
      "include": "#inline"
    }
  ],
  "repository": {
    "frontmatter": {
      "begin": "\\A\\x{FEFF}?---\\s*$",
      "end": "^---\\s*$",
      "name": "meta.frontmatter.carve",
      "beginCaptures": {
        "0": {
          "name": "punctuation.definition.frontmatter.carve"
        }
      },
      "endCaptures": {
        "0": {
          "name": "punctuation.definition.frontmatter.carve"
        }
      },
      "patterns": [
        {
          "match": "^([A-Za-z_][\\w-]*)\\s*(:)",
          "captures": {
            "1": {
              "name": "entity.name.tag.yaml.carve"
            },
            "2": {
              "name": "punctuation.separator.key-value.yaml.carve"
            }
          }
        }
      ]
    },
    "fenced_code": {
      "begin": "^(?:\\A\\x{FEFF})?()(`{3,}|~{3,})\\s*(=?[a-zA-Z0-9_-]*)\\s*(\"[^\"\\n]*\")?\\s*(\\[[^\\]\\n]*\\])?\\s*$",
      "beginCaptures": {
        "2": {
          "name": "punctuation.definition.fenced.carve"
        },
        "3": {
          "name": "fenced_code.block.language.carve"
        },
        "4": {
          "name": "string.quoted.double.fenced.title.carve"
        },
        "5": {
          "name": "constant.other.label.fenced.carve"
        }
      },
      "end": "^\\1\\2\\s*$",
      "endCaptures": {
        "0": {
          "name": "punctuation.definition.fenced.carve"
        }
      },
      "name": "markup.fenced_code.block.carve",
      "contentName": "markup.raw.code.carve",
      "patterns": [
        {
          "include": "#callout_marker"
        }
      ],
      "comment": "The DOCUMENT-LEVEL fence, anchored at column 0 (carve-grammars#138). An indented opener at the top level is literal text - verified against the pinned engine, ` ```js` renders `<p><code>js …</code></p>` - so this rule refuses it and `#fenced_code_in_container` takes over inside a container. Group 1 is an EMPTY capture rather than a dropped one: `end` closes on the opener's own indentation via `^\\1\\2`, and keeping the group means this rule and its in-container twin differ by exactly that one anchor, with every capture index and the `end` pattern identical. Two rules that differ in one place cannot drift the way two rules that differ in five can."
    },
    "fenced_code_in_container": {
      "begin": "^(\\s*)(`{3,}|~{3,})\\s*(=?[a-zA-Z0-9_-]*)\\s*(\"[^\"\\n]*\")?\\s*(\\[[^\\]\\n]*\\])?\\s*$",
      "beginCaptures": {
        "2": {
          "name": "punctuation.definition.fenced.carve"
        },
        "3": {
          "name": "fenced_code.block.language.carve"
        },
        "4": {
          "name": "string.quoted.double.fenced.title.carve"
        },
        "5": {
          "name": "constant.other.label.fenced.carve"
        }
      },
      "end": "^\\1\\2\\s*$",
      "endCaptures": {
        "0": {
          "name": "punctuation.definition.fenced.carve"
        }
      },
      "name": "markup.fenced_code.block.carve",
      "contentName": "markup.raw.code.carve",
      "patterns": [
        {
          "include": "#callout_marker"
        }
      ],
      "comment": "The in-container fence: leading whitespace is the container's own indent, so it is allowed here and only here. `#container_blocks` reaches this; the leading-whitespace capture stays load-bearing twice over, since `end` closes on the SAME indentation via \\1 and a fence inside a list item is indented. Reached only after the container's `end` has established that the line sits at or beyond the item's content column, which is the block context a line-based grammar cannot derive on its own (carve-grammars#71, #137)."
    },
    "heading": {
      "begin": "^(?:\\A\\x{FEFF})?(#{1,6}) +(?![ \\t]*$)",
      "beginCaptures": {
        "1": {
          "name": "punctuation.definition.heading.carve"
        }
      },
      "end": "$",
      "name": "markup.heading.carve",
      "contentName": "entity.name.section.carve",
      "patterns": [
        {
          "include": "#heading_tag"
        }
      ],
      "comment": "A heading opens at COLUMN 0 at document level. Leading whitespace used to be allowed here (carve-grammars#71) because a line-based grammar could not tell an opener at a list item's content column from an indented one at the top level - it over-coloured the rare invalid case rather than under-colour the common valid one. The container split (carve-grammars#137) removed that constraint: `#heading_in_container` is the permissive variant, reachable only through `#container_blocks`, so the document-level rule can be strict. Corpus 178-a-flush-left-line-needs-an-open-paragraph-to-fold-into-6 pins the case this fixes: ` # H` below a nested item's content column is paragraph text, and this grammar coloured it a heading (carve-grammars#138)."
    },
    "hr": {
      "match": "^(?:\\A\\x{FEFF})?\\s*(?:-{3,}|\\*{3,}|_{3,})\\s*$",
      "name": "meta.separator.thematic-break.carve"
    },
    "block_comment": {
      "comment": "A %%% fence line is a delimiter plus an insignificant tail (spec PART 9 section 28): only the leading run of % is structural, so `%%% TODO` opens and `%%% end` closes. %%% carries no info string - a raw passthrough block is a CODE fence with an =FORMAT info string - so `%%% html` is a comment. The end backreference matches the opener width exactly, so %%%% nests %%%.",
      "begin": "^(?:\\A\\x{FEFF})?\\s*(%{3,})[^\\n]*$",
      "end": "^\\s*\\1(?!%)[^\\n]*$",
      "name": "comment.block.carve"
    },
    "braced_comment": {
      "comment": "THE PAYLOAD IS NOT CARVE AND MAY SPAN A LINE BREAK. This was a single-line `match`, so a comment written across a break was not recognized at all and the markup inside it coloured - `a {% *b*` over ` %} z` scoped a strong run inside a comment, which is markup-carve/carve#1239 on the surface it was first reported on. 144 of 469 generated documents leaked before this (carve-grammars#320). Prism and highlight.js were given the multi-line reading in carve-grammars#312; this is the same fix on the TextMate side. The region carries the scope and offers no patterns, so everything between the delimiters is comment. A BLANK LINE ENDS IT, which is the bound Prism already uses for this construct - its pattern spells the body as a newline that is not followed by a blank line. A TextMate begin cannot look ahead for its own closer, because the regex sees one line, so without a bound an unclosed opener would scope the rest of the document: the failure the seven recorded unterminated runs in tests/lib/unterminated-fences.js describe. One paragraph is where the engine stops reading this construct too.",
      "begin": "\\{%",
      "end": "%\\}|^(?=[ \\t]*$)",
      "name": "comment.block.inline.carve"
    },
    "line_comment": {
      "match": "^(?:\\A\\x{FEFF})?\\s*%%(?!%).*$",
      "name": "comment.line.percent.carve"
    },
    "trailing_comment": {
      "match": "(?<=[ \\t])%%.*$",
      "name": "comment.line.percent.carve"
    },
    "admonition": {
      "begin": "^(?:\\A\\x{FEFF})?[ \\t]*(:{3,})(?:[ \\t]*(\\||\\\\)|[ \\t]*([a-zA-Z_][\\w-]*)(?:[ \\t]+(\"[^\"\\n]*\"))?(?:[ \\t]+(\\[[^\\]\\n]*\\]))?|[ \\t]*(\\[[^\\]\\n]*\\]))?[ \\t]*$",
      "beginCaptures": {
        "1": {
          "name": "punctuation.definition.admonition.carve"
        },
        "2": {
          "name": "entity.name.tag.admonition.carve"
        },
        "3": {
          "name": "entity.name.tag.admonition.carve"
        },
        "4": {
          "name": "string.quoted.double.admonition.title.carve"
        },
        "5": {
          "name": "constant.other.label.admonition.carve"
        },
        "6": {
          "name": "constant.other.label.admonition.carve"
        }
      },
      "end": "^[ \\t]*\\1[ \\t]*$",
      "endCaptures": {
        "0": {
          "name": "punctuation.definition.admonition.carve"
        }
      },
      "name": "meta.admonition.carve",
      "patterns": [
        {
          "include": "#fenced_code"
        },
        {
          "include": "#heading"
        },
        {
          "include": "#hr"
        },
        {
          "include": "#block_comment"
        },
        {
          "include": "#line_comment"
        },
        {
          "include": "#figure_group"
        },
        {
          "include": "#admonition"
        },
        {
          "include": "#block_comment_on_quote_marker_line"
        },
        {
          "include": "#blockquote"
        },
        {
          "include": "#link_ref_def_in_container"
        },
        {
          "include": "#caption"
        },
        {
          "include": "#definition_list"
        },
        {
          "include": "#table_row"
        },
        {
          "include": "#list_continuation"
        },
        {
          "include": "#multiline_cell"
        },
        {
          "include": "#callout_annotation"
        },
        {
          "include": "#task_item"
        },
        {
          "include": "#list_item"
        },
        {
          "include": "#numbered_item"
        },
        {
          "include": "#inline"
        }
      ],
      "comment": "Leading whitespace is ALLOWED, deliberately (carve-grammars#71). The strict column-0 rule makes an indented opener literal text at the top level, but the same opener indented INSIDE a list item is valid and common - verified against carve-rs: at the top level every one of these renders as a paragraph, and at a list item's content column every one opens its real block. List items now provide the block context needed for that distinction: their begin/end rules keep container-only block patterns reachable at the item's content column and close them before a below-content-column line falls back to document text. Restructured from a flat `match` into `begin`/`end` (carve-grammars#125): a div/admonition's opener and closer are on their own lines, so a per-line `match` left the body wide open to the full top-level pattern set - which incorrectly let `#abbreviation` fire inside a div (PART 9: abbreviation definitions are recognized at document level only; the sibling list-item and blockquote cases already read correctly, but only because their own line mechanics happen to block it, not because either detects the container). The closer backreferences the opener's captured colon run (`\\1`), so it closes on an EXACT length match, matching PART 9's colon-fence depth rule (a longer or shorter run does not close it, which is what lets equal-length fences nest and `::::` hold `:::`) - mirrors the `fenced_code` begin/end precedent in this same file. `patterns` is the top-level pattern list minus `#frontmatter` (document-start only) and minus `#abbreviation` (the one construct this fix targets); everything else - headings (corpus 170), nested admonitions/divs via the `#admonition` self-include, lists, blockquotes, tables - keeps working inside a div exactly as before."
    },
    "admonition_in_group": {
      "begin": "^(?:\\A\\x{FEFF})?[ \\t]*(:{3,})(?:[ \\t]*(\\||\\\\)|[ \\t]*([a-zA-Z_][\\w-]*)(?:[ \\t]+(\"[^\"\\n]*\"))?(?:[ \\t]+(\\[[^\\]\\n]*\\]))?|[ \\t]*(\\[[^\\]\\n]*\\]))?[ \\t]*$",
      "beginCaptures": {
        "1": {
          "name": "punctuation.definition.admonition.carve"
        },
        "2": {
          "name": "entity.name.tag.admonition.carve"
        },
        "3": {
          "name": "entity.name.tag.admonition.carve"
        },
        "4": {
          "name": "string.quoted.double.admonition.title.carve"
        },
        "5": {
          "name": "constant.other.label.admonition.carve"
        },
        "6": {
          "name": "constant.other.label.admonition.carve"
        }
      },
      "end": "^[ \\t]*\\1[ \\t]*$",
      "endCaptures": {
        "0": {
          "name": "punctuation.definition.admonition.carve"
        }
      },
      "name": "meta.admonition.carve",
      "patterns": [
        {
          "include": "#fenced_code"
        },
        {
          "include": "#heading"
        },
        {
          "include": "#hr"
        },
        {
          "include": "#block_comment"
        },
        {
          "include": "#line_comment"
        },
        {
          "include": "#admonition_in_group"
        },
        {
          "include": "#block_comment_on_quote_marker_line"
        },
        {
          "include": "#blockquote"
        },
        {
          "include": "#link_ref_def_in_container"
        },
        {
          "include": "#caption"
        },
        {
          "include": "#definition_list"
        },
        {
          "include": "#table_row"
        },
        {
          "include": "#list_continuation"
        },
        {
          "include": "#multiline_cell"
        },
        {
          "include": "#callout_annotation"
        },
        {
          "include": "#task_item"
        },
        {
          "include": "#list_item"
        },
        {
          "include": "#numbered_item"
        },
        {
          "include": "#inline"
        }
      ],
      "comment": "The generic container, reached only from INSIDE a composite figure group. Identical to `#admonition` except that its body offers `#admonition_in_group` and never `#figure_group`, so a bare `::: figure` at any depth inside an open group scopes as the generic container the clause says it degrades to (PART 9 §4c, GROUPS DO NOT NEST; corpus 318-composite-figures-9). Scope names are deliberately unchanged - it IS an admonition, and a consumer selecting `meta.admonition.carve` must keep seeing it."
    },
    "figure_group": {
      "begin": "^(?:\\A\\x{FEFF})?[ \\t]*(:{3,}) +(figure)[ \\t]*$",
      "beginCaptures": {
        "1": {
          "name": "punctuation.definition.figure-group.carve"
        },
        "2": {
          "name": "entity.name.tag.figure-group.carve"
        }
      },
      "end": "^[ \\t]*\\1[ \\t]*$",
      "endCaptures": {
        "0": {
          "name": "punctuation.definition.figure-group.carve"
        }
      },
      "name": "meta.figure-group.carve",
      "patterns": [
        {
          "include": "#fenced_code"
        },
        {
          "include": "#heading"
        },
        {
          "include": "#hr"
        },
        {
          "include": "#block_comment"
        },
        {
          "include": "#line_comment"
        },
        {
          "include": "#admonition_in_group"
        },
        {
          "include": "#block_comment_on_quote_marker_line"
        },
        {
          "include": "#blockquote"
        },
        {
          "include": "#link_ref_def_in_container"
        },
        {
          "include": "#caption"
        },
        {
          "include": "#definition_list"
        },
        {
          "include": "#table_row"
        },
        {
          "include": "#list_continuation"
        },
        {
          "include": "#multiline_cell"
        },
        {
          "include": "#callout_annotation"
        },
        {
          "include": "#task_item"
        },
        {
          "include": "#list_item"
        },
        {
          "include": "#numbered_item"
        },
        {
          "include": "#inline"
        }
      ],
      "comment": "Composite figure (PART 9 §4c, markup-carve/carve#1215). The kind word `figure` is RESERVED among the `:::` types: a BARE opener - the fence, its separator, the word `figure`, and NOTHING else - is ONE figure of ordered panels, not an admonition, so it carries its own scope and this rule sits BEFORE `#admonition` everywhere the generic container is reachable. The `[ \\t]*$` tail is the whole distinction: an opener carrying a quoted title or a `[label]` (`::: figure \"T\"`, `::: figure [g]`) does not match here at all and falls through to `#admonition`, which is the generic Tier-2 container the clause says it stays. GROUPS DO NOT NEST: a bare `::: figure` at ANY depth inside an open group is a generic container, so this rule's body reaches `#admonition_in_group` rather than `#admonition` - that variant is the same generic container with `#figure_group` removed from its own body, which is what carries the rule down a chain of `:::` containers. RESIDUAL, written down rather than left to be rediscovered: a bare opener reached through a LIST ITEM or a BLOCKQUOTE inside a group goes through `#container_blocks`, which still offers `#figure_group`, so it over-colours as a group there. That is the same block-context limit every line-based rule in this file has (carve-grammars#71); tree-sitter-carve is where a real container model lives. THE SEPARATOR IS A SPACE RUN, never a tab (grammar.ebnf PART 7, MARKER SEPARATORS; corpus 254): `:::<TAB>figure` is a paragraph, so this rule does not claim it and the line falls to `#admonition`, which over-colours it exactly as it does today - a pre-existing trade this rule neither widens nor fixes. Trailing whitespace after the kind word is insignificant and may be a tab. The closer backreferences the opener's colon run (`\\1`), the exact-length rule `#admonition` already follows. THE CAPTION AFTER THE CLOSER needs no rule here: the group caption is an ordinary `^ ` line one line below the closing fence, which `#caption` already claims at the document level."
    },
    "blockquote": {
      "match": "^(?:\\A\\x{FEFF})?(>)(?= |$) ?(.*)$",
      "captures": {
        "1": {
          "name": "punctuation.definition.quote.carve"
        },
        "2": {
          "name": "markup.quote.carve"
        }
      },
      "name": "markup.quote.carve",
      "comment": "The DOCUMENT-LEVEL blockquote, anchored at column 0 (carve-grammars#138). An indented marker at the top level is literal text - verified against the pinned engine, ` > q` renders `<p>&gt; q</p>` - so this rule refuses it and `#blockquote_in_container` takes over inside a container."
    },
    "blockquote_in_container": {
      "match": "^[ \\t]*(>)(?= |$) ?(.*)$",
      "captures": {
        "1": {
          "name": "punctuation.definition.quote.carve"
        },
        "2": {
          "name": "markup.quote.carve"
        }
      },
      "name": "markup.quote.carve",
      "comment": "The in-container blockquote: leading whitespace is the container's own indent, so it is allowed here and only here. `#container_blocks` reaches this, and only after the container's `end` has established that the line sits at or beyond the item's content column - which is the block context a line-based grammar cannot derive on its own (carve-grammars#71, #137)."
    },
    "caption": {
      "match": "^(?:\\A\\x{FEFF})?[ \\t]*(\\^) +(?![ \\t]*$)(.*)$",
      "captures": {
        "1": {
          "name": "punctuation.definition.caption.carve"
        },
        "2": {
          "name": "markup.caption.carve"
        }
      },
      "name": "meta.caption.carve",
      "comment": "Leading whitespace is ALLOWED, deliberately (carve-grammars#71). The strict column-0 rule makes an indented opener literal text at the top level, but the same opener indented INSIDE a list item is valid and common - verified against carve-rs: at the top level every one of these renders as a paragraph, and at a list item's content column every one opens its real block. A line-based grammar cannot tell those two apart; that needs block context, which only tree-sitter-carve has (its #38). So the choice is over-coloring the rare invalid case or under-coloring the common valid one, and every block rule here picks the same side - the asymmetry this used to have was an accident of anchoring, not a decision."
    },
    "abbreviation": {
      "match": "^(?:\\A\\x{FEFF})?(\\*)(\\[)([A-Za-z0-9]+)(\\])(:) +(.*)$",
      "captures": {
        "1": {
          "name": "punctuation.definition.abbreviation.carve"
        },
        "2": {
          "name": "punctuation.definition.abbreviation.carve"
        },
        "3": {
          "name": "entity.name.abbreviation.carve"
        },
        "4": {
          "name": "punctuation.definition.abbreviation.carve"
        },
        "5": {
          "name": "punctuation.separator.carve"
        },
        "6": {
          "name": "string.unquoted.abbreviation.carve"
        }
      },
      "name": "meta.abbreviation.carve",
      "comment": "The DOCUMENT-LEVEL abbreviation definition, anchored at column 0 (carve-grammars#138). An indented definition at the top level is literal text - verified against the pinned engine, ` *[HTML]: HyperText` renders `<p>*[HTML]: HyperText</p>` and defines nothing - so this rule refuses it and `#abbreviation_in_container` takes over inside a container."
    },
    "abbreviation_in_container": {
      "match": "^[ \\t]*(\\*)(\\[)([A-Za-z0-9]+)(\\])(:) +(.*)$",
      "captures": {
        "1": {
          "name": "punctuation.definition.abbreviation.carve"
        },
        "2": {
          "name": "punctuation.definition.abbreviation.carve"
        },
        "3": {
          "name": "entity.name.abbreviation.carve"
        },
        "4": {
          "name": "punctuation.definition.abbreviation.carve"
        },
        "5": {
          "name": "punctuation.separator.carve"
        },
        "6": {
          "name": "string.unquoted.abbreviation.carve"
        }
      },
      "name": "meta.abbreviation.carve",
      "comment": "The in-container abbreviation definition: leading whitespace is the container's own indent, so it is allowed here and only here. `#container_blocks` reaches this; `#admonition` deliberately does NOT (PART 9 recognizes abbreviation definitions at document level, and carve-grammars#125 removed the div's include for exactly that reason). NOTE the standing over-colour this variant keeps: the pinned engine does not collect an abbreviation inside a list item either - `- item\\n\\n  *[HTML]: HyperText` renders the definition line as item text - so this is the same rare-wrong-answer trade carve-grammars#89 chose, kept here rather than widened or narrowed by a ticket about document-level anchors."
    },
    "link_ref_def": {
      "match": "^(?:\\A\\x{FEFF})?(\\[)(?![@^])([^\\]]+)(\\])(:) +(\\S+)(?:[ \\t]+(\"(?:[^\"\\\\]|\\\\.)*\"|'(?:[^'\\\\]|\\\\.)*'))?",
      "captures": {
        "1": {
          "name": "punctuation.definition.link.carve"
        },
        "2": {
          "name": "constant.other.reference.link.carve"
        },
        "3": {
          "name": "punctuation.definition.link.carve"
        },
        "4": {
          "name": "punctuation.separator.carve"
        },
        "5": {
          "name": "markup.underline.link.carve"
        },
        "6": {
          "name": "string.quoted.link.title.carve"
        }
      },
      "name": "meta.link.reference.def.carve",
      "comment": "At document level a link-reference definition is flush-left. Indented definitions are reached through container patterns, where the container's begin/end rule has already established that the line is at that block's content column."
    },
    "link_ref_def_in_container": {
      "match": "^([ \\t]*)(\\[)(?![@^])([^\\]]+)(\\])(:) +(\\S+)(?:[ \\t]+(\"(?:[^\"\\\\]|\\\\.)*\"|'(?:[^'\\\\]|\\\\.)*'))?",
      "captures": {
        "2": {
          "name": "punctuation.definition.link.carve"
        },
        "3": {
          "name": "constant.other.reference.link.carve"
        },
        "4": {
          "name": "punctuation.definition.link.carve"
        },
        "5": {
          "name": "punctuation.separator.carve"
        },
        "6": {
          "name": "markup.underline.link.carve"
        },
        "7": {
          "name": "string.quoted.link.title.carve"
        }
      },
      "name": "meta.link.reference.def.carve"
    },
    "table_row": {
      "begin": "^(?:\\A\\x{FEFF})?[ \\t]*(?=\\|)",
      "end": "$",
      "patterns": [
        {
          "match": "(\\|=)((?:[<~>][\\^~v]|[<~>]))(?= |\\{)",
          "captures": {
            "1": {
              "name": "keyword.operator.table.header.carve"
            },
            "2": {
              "name": "keyword.operator.table.alignment.carve"
            }
          }
        },
        {
          "match": "(\\|=)",
          "captures": {
            "1": {
              "name": "keyword.operator.table.header.carve"
            }
          }
        },
        {
          "match": "(\\|)((?:[<~>][\\^~v]|[<~>]))(?= |\\{)",
          "captures": {
            "1": {
              "name": "punctuation.separator.table.carve"
            },
            "2": {
              "name": "keyword.operator.table.alignment.carve"
            }
          }
        },
        {
          "match": "(?<=\\|)\\s*(\\^)\\s*(?=\\|)",
          "captures": {
            "1": {
              "name": "keyword.operator.table.rowspan.carve"
            }
          }
        },
        {
          "match": "(?<=\\|)\\s*(<)\\s*(?=\\|)",
          "captures": {
            "1": {
              "name": "keyword.operator.table.colspan.carve"
            }
          }
        },
        {
          "comment": "Column alignment markers (:--, --:, :-:, ---) between pipes. Matched before #inline so smart-typography does not swallow the dashes.",
          "match": "(?<=\\|)\\s*(:?-+:?)\\s*(?=\\|)",
          "captures": {
            "1": {
              "name": "keyword.operator.table.alignment.carve"
            }
          }
        },
        {
          "match": "\\|",
          "name": "punctuation.separator.table.carve"
        },
        {
          "include": "#inline"
        }
      ],
      "comment": "Leading whitespace is ALLOWED, deliberately (carve-grammars#71). The strict column-0 rule makes an indented opener literal text at the top level, but the same opener indented INSIDE a list item is valid and common - verified against carve-rs: at the top level every one of these renders as a paragraph, and at a list item's content column every one opens its real block. A line-based grammar cannot tell those two apart; that needs block context, which only tree-sitter-carve has (its #38). So the choice is over-coloring the rare invalid case or under-coloring the common valid one, and every block rule here picks the same side - the asymmetry this used to have was an accident of anchoring, not a decision."
    },
    "multiline_cell": {
      "match": "^(?:\\A\\x{FEFF})?[ \\t]*(\\+)(.*)$",
      "captures": {
        "1": {
          "name": "keyword.operator.table.continuation.carve"
        }
      },
      "comment": "Leading whitespace is ALLOWED, deliberately (carve-grammars#71). The strict column-0 rule makes an indented opener literal text at the top level, but the same opener indented INSIDE a list item is valid and common - verified against carve-rs: at the top level every one of these renders as a paragraph, and at a list item's content column every one opens its real block. A line-based grammar cannot tell those two apart; that needs block context, which only tree-sitter-carve has (its #38). So the choice is over-coloring the rare invalid case or under-coloring the common valid one, and every block rule here picks the same side - the asymmetry this used to have was an accident of anchoring, not a decision."
    },
    "task_item": {
      "comment": "A task item is a bullet whose content happens to start with a checkbox, and ITS CONTENT COLUMN IS TWO PAST THE INDENT WHATEVER THE SEPARATOR IS - the checkbox absorbs the rest. That is measured, not assumed: carve-php and carve-js agree that a body line two columns in belongs to the item for every separator width from one space to five, where the same widths under a plain bullet carry the column with them (`-   a` puts its body at column 4, `-   [x] a` keeps it at column 2). A definition two columns under `- [ ] a` is collected exactly as it is under `- a`, which is why the checkbox counts as CONTENT rather than as part of the marker, and why `end` asks for a fixed two columns here while `#list_item` reconstructs the separator width from the marker line. The rule is a container in its own right for the same reason: a one-line match would leave the following lines to the document level, where a definition must be flush-left, and `- [ ] a` would lose the indented definition that `- a` keeps.",
      "begin": "^(?:\\A\\x{FEFF})?([ \\t]*)([-*]) +(\\[)([ xX\\-_>?])(\\]) +(?![ \\t]*$)",
      "beginCaptures": {
        "2": {
          "name": "punctuation.definition.list.carve"
        },
        "3": {
          "name": "punctuation.definition.checkbox.carve"
        },
        "4": {
          "name": "constant.language.checkbox.carve"
        },
        "5": {
          "name": "punctuation.definition.checkbox.carve"
        }
      },
      "end": "^(?![ \\t]*$)(?!\\1[ \\t]{2})",
      "patterns": [
        {
          "include": "#container_blocks"
        }
      ]
    },
    "list_continuation": {
      "comment": "A lone `+` at the list-marker column is the list-continuation marker (not a bullet -- `+` is never a bullet in Carve); it attaches the following flush-left block to the item.",
      "match": "^(?:\\A\\x{FEFF})?(\\s*)(\\+)(\\s*)$",
      "captures": {
        "2": {
          "name": "punctuation.definition.list.continuation.carve"
        }
      }
    },
    "list_item": {
      "comment": "A LIST ITEM IS A CONTAINER, NOT A LINE (carve-grammars#94). It opens on its marker line, stays open across blank lines, and closes on the first nonblank line indented BELOW its content column - which is the marker's actual width, `indent + marker + separator`, not a fixed two columns. That column test is what lets the block rules inside it (`#container_blocks`) fire on an indented line without also firing on a top-level indented line that the language reads as text: `- a` followed by `  [r]: /u` keeps the definition, while `- - a` followed by ` [r]: /u` sits below every open content column and is item text, which is what carve-php does with both. THE TEST IS EXACT FOR A BULLET at any indent and any separator width, because a bullet marker is always one column wide and the separator is captured as whitespace, so `\\1[ \\t]\\3` reconstructs the column count from the marker line itself rather than assuming one. Its limits: a TAB counts as one column here rather than advancing to the next tab stop, and the continuation line must repeat the marker line's own indent characters - the pre-existing `\\1` convention, unchanged. The ATTRIBUTE-GLUED FORM (`-{#x} item`) is the one bullet shape whose content column this cannot count, since the block's width is not whitespace; it uses the MINIMUM content column (indent plus two) instead, which keeps the container open across a range where an exact test would have closed it - deliberately the over-coloring side, the same side every block rule in this file already takes. A marker line may carry several markers (`- - A` opens nested list structure), so the opener still scopes a run of same-line bullet markers, not only the line-anchored first, and the column test uses the OUTERMOST marker's column so a line at the outer item's column stays in the list. A BULLET MAY ALSO BE GLUED TO AN ATTRIBUTE BLOCK, and then the required space comes after the block, not after the marker - so `-{#x} item` and `*{.c} item` went uncoloured on a line that IS a list item, while the ordered rule beside it already had the branch (#126). The guard is that rule's, unchanged: content is still required past the block (`-{#x}` alone is a paragraph), and the block is spelled out rather than skipped with `\\{[^}]*\\}`, because a quoted value may hold a `}` and may escape its own quote. It is a LOOKAHEAD - consuming the block here would make this rule own those characters, so the attribute rule could no longer scope them. THE PAYLOAD MUST BE VALID, not merely brace-delimited: an invalid one means the `{` is literal content and the line is prose. `-{+a+} text` (corpus 90-list-item-attributes-6) and `1.{2=v} text` are paragraphs, while `-{} item` and `-{not attrs} item` are list items. A guard that accepted any brace run coloured the first two as lists. Identifiers are STRICT (PART 9 S14) and admit no colon, which is what carve-js does - Prism and highlight.js still admit one in their own attribute rules, a separate pre-existing divergence.",
      "patterns": [
        {
          "begin": "^(?:\\A\\x{FEFF})?([ \\t]*)([-*])( +)(?![ \\t]*$)((?:[-*] +)*)",
          "beginCaptures": {
            "2": {
              "name": "punctuation.definition.list.unnumbered.carve"
            },
            "4": {
              "name": "punctuation.definition.list.unnumbered.carve"
            }
          },
          "end": "^(?![ \\t]*$)(?!\\1[ \\t]\\3)",
          "patterns": [
            {
              "include": "#container_blocks"
            }
          ]
        },
        {
          "begin": "^(?:\\A\\x{FEFF})?([ \\t]*)([-*])(?=\\{\\s*(?:(?::(?:[A-Za-z0-9]{1,8}(?:-[A-Za-z0-9]{1,8})*)?|[.#][A-Za-z_][\\w-]*|[A-Za-z_][\\w-]*(?:=(?:\"(?:[^\"\\\\\\n]|\\\\.)*\"|'(?:[^'\\\\\\n]|\\\\.)*'|[^\\s\"'{}]+))?)(?:\\s+(?::(?:[A-Za-z0-9]{1,8}(?:-[A-Za-z0-9]{1,8})*)?|[.#][A-Za-z_][\\w-]*|[A-Za-z_][\\w-]*(?:=(?:\"(?:[^\"\\\\\\n]|\\\\.)*\"|'(?:[^'\\\\\\n]|\\\\.)*'|[^\\s\"'{}]+))?))*\\s*)?\\}[ \\t]+[^ \\t\\n])",
          "beginCaptures": {
            "2": {
              "name": "punctuation.definition.list.unnumbered.carve"
            }
          },
          "end": "^(?![ \\t]*$)(?!\\1[ \\t]{2,})",
          "patterns": [
            {
              "include": "#container_blocks"
            }
          ]
        }
      ]
    },
    "numbered_item": {
      "comment": "AN ORDERED ITEM IS A CONTAINER, NOT A LINE (carve-grammars#94), on the same content-column rule as `#list_item`: it closes on the first nonblank line indented below `indent + marker + separator`. An ordered marker is not one column wide though, and Oniguruma cannot count the width of a captured run - vscode-textmate splices a begin capture into the `end` pattern as literal TEXT, so a backreference reproduces the marker's characters, never its length. The width is therefore spelled out: one variant per marker width, each consuming exactly that many characters and asking its `end` for exactly that many columns. EXACT for marker widths 1 to 5 - `.`, `1.`, `10.`, `iii.`, `1000.` - which covers every ordered marker in the corpus; the width-5 variant also accepts anything WIDER (`10000.`, `civil.`) and under-counts those by the extra characters, keeping the container open a column or two too long rather than closing it too early. APPROXIMATE for the attribute-glued form (`1.{#x} item`), whose block width is not whitespace either: it uses the minimum content column, indent plus two. Both approximations err toward over-coloring, the same side every block rule in this file already takes. Every ordered marker the language accepts is still here, not only `1.`: decimal, single-letter and roman, with `.` or `)`, plus the BARE DOT (carve#472) - `.` alone continues the ordered sequence, and is the only marker allowed to drop its value. The bare form is also recognised glued to an attribute block (`.{#x}`), which is how corpus 174-bare-dot-ordered-markers-3 writes it. MARKER REQUIRES CONTENT, AND SO DOES THE ATTRIBUTE FORM: `1.{#x}` with nothing after the block is a paragraph, so the glued-block branch spells the block out in full rather than skipping it - a quoted run may contain `}` (`{title=\"a}b\"} x` is a valid item) and may escape its own quote, so a `\\{[^}]*\\}` run stops in the wrong place. Anything less was tried and each attempt traded one broken shape for another (#85). A ROMAN RUN IS CASE-CONSISTENT: two classes, not one `[ivxlcdmIVXLCDM]`, which matched any mixture and coloured `Vim. text` and `Mix. text` as lists where carve-js renders paragraphs (#118). `mix.`, `civil.` and `did.` DO open lists, so the fix is the case split rather than rejecting multi-letter words. THE PAYLOAD MUST BE VALID, not merely brace-delimited: an invalid one means the `{` is literal content and the line is prose. `-{+a+} text` (corpus 90-list-item-attributes-6) and `1.{2=v} text` are paragraphs, while `-{} item` and `-{not attrs} item` are list items. A guard that accepted any brace run coloured the first two as lists. Identifiers are STRICT (PART 9 S14) and admit no colon, which is what carve-js does - Prism and highlight.js still admit one in their own attribute rules, a separate pre-existing divergence.",
      "patterns": [
        {
          "begin": "^(?:\\A\\x{FEFF})?([ \\t]*)(\\.)( +)(?![ \\t]*$)",
          "beginCaptures": {
            "2": {
              "name": "punctuation.definition.list.numbered.carve"
            }
          },
          "end": "^(?![ \\t]*$)(?!\\1[ \\t]\\3)",
          "patterns": [
            {
              "include": "#container_blocks"
            }
          ]
        },
        {
          "begin": "^(?:\\A\\x{FEFF})?([ \\t]*)((?:\\d|[A-Za-z])[.)])( +)(?![ \\t]*$)",
          "beginCaptures": {
            "2": {
              "name": "punctuation.definition.list.numbered.carve"
            }
          },
          "end": "^(?![ \\t]*$)(?!\\1[ \\t][ \\t]\\3)",
          "patterns": [
            {
              "include": "#container_blocks"
            }
          ]
        },
        {
          "begin": "^(?:\\A\\x{FEFF})?([ \\t]*)((?:\\d{2}|[ivxlcdm]{2}|[IVXLCDM]{2})[.)])( +)(?![ \\t]*$)",
          "beginCaptures": {
            "2": {
              "name": "punctuation.definition.list.numbered.carve"
            }
          },
          "end": "^(?![ \\t]*$)(?!\\1[ \\t][ \\t][ \\t]\\3)",
          "patterns": [
            {
              "include": "#container_blocks"
            }
          ]
        },
        {
          "begin": "^(?:\\A\\x{FEFF})?([ \\t]*)((?:\\d{3}|[ivxlcdm]{3}|[IVXLCDM]{3})[.)])( +)(?![ \\t]*$)",
          "beginCaptures": {
            "2": {
              "name": "punctuation.definition.list.numbered.carve"
            }
          },
          "end": "^(?![ \\t]*$)(?!\\1[ \\t][ \\t][ \\t][ \\t]\\3)",
          "patterns": [
            {
              "include": "#container_blocks"
            }
          ]
        },
        {
          "begin": "^(?:\\A\\x{FEFF})?([ \\t]*)((?:\\d{4,}|[ivxlcdm]{4,}|[IVXLCDM]{4,})[.)])( +)(?![ \\t]*$)",
          "beginCaptures": {
            "2": {
              "name": "punctuation.definition.list.numbered.carve"
            }
          },
          "end": "^(?![ \\t]*$)(?!\\1[ \\t][ \\t][ \\t][ \\t][ \\t]\\3)",
          "patterns": [
            {
              "include": "#container_blocks"
            }
          ]
        },
        {
          "begin": "^(?:\\A\\x{FEFF})?([ \\t]*)((?:\\d+|[A-Za-z]|[ivxlcdm]+|[IVXLCDM]+)[.)]|\\.)(?=\\{\\s*(?:(?::(?:[A-Za-z0-9]{1,8}(?:-[A-Za-z0-9]{1,8})*)?|[.#][A-Za-z_][\\w-]*|[A-Za-z_][\\w-]*(?:=(?:\"(?:[^\"\\\\\\n]|\\\\.)*\"|'(?:[^'\\\\\\n]|\\\\.)*'|[^\\s\"'{}]+))?)(?:\\s+(?::(?:[A-Za-z0-9]{1,8}(?:-[A-Za-z0-9]{1,8})*)?|[.#][A-Za-z_][\\w-]*|[A-Za-z_][\\w-]*(?:=(?:\"(?:[^\"\\\\\\n]|\\\\.)*\"|'(?:[^'\\\\\\n]|\\\\.)*'|[^\\s\"'{}]+))?))*\\s*)?\\}[ \\t]+[^ \\t\\n])",
          "beginCaptures": {
            "2": {
              "name": "punctuation.definition.list.numbered.carve"
            }
          },
          "end": "^(?![ \\t]*$)(?!\\1[ \\t]{2,})",
          "patterns": [
            {
              "include": "#container_blocks"
            }
          ]
        }
      ]
    },
    "inline": {
      "patterns": [
        {
          "include": "#math_inline"
        },
        {
          "include": "#literal_inline_multi"
        },
        {
          "include": "#literal_inline"
        },
        {
          "include": "#footnote_inline"
        },
        {
          "include": "#code_inline_multi"
        },
        {
          "include": "#code_inline"
        },
        {
          "include": "#code_inline_unpartnered"
        },
        {
          "include": "#braced_comment"
        },
        {
          "include": "#trailing_comment"
        },
        {
          "include": "#image"
        },
        {
          "include": "#image_ref"
        },
        {
          "include": "#citation"
        },
        {
          "include": "#span"
        },
        {
          "include": "#link_inline"
        },
        {
          "include": "#link_ref"
        },
        {
          "include": "#extension_inline"
        },
        {
          "include": "#symbol"
        },
        {
          "include": "#critic_add"
        },
        {
          "include": "#critic_del"
        },
        {
          "include": "#critic_sub"
        },
        {
          "include": "#critic_comment"
        },
        {
          "include": "#highlight"
        },
        {
          "include": "#subscript"
        },
        {
          "include": "#superscript"
        },
        {
          "include": "#forced_strong"
        },
        {
          "include": "#forced_italic"
        },
        {
          "include": "#forced_underline"
        },
        {
          "include": "#forced_strike"
        },
        {
          "include": "#attributes_empty"
        },
        {
          "include": "#attributes_multiline"
        },
        {
          "include": "#attributes"
        },
        {
          "include": "#bold_italic"
        },
        {
          "include": "#strong"
        },
        {
          "include": "#italic"
        },
        {
          "include": "#underline"
        },
        {
          "include": "#strike"
        },
        {
          "include": "#mention"
        },
        {
          "include": "#tag"
        },
        {
          "include": "#cross_ref"
        },
        {
          "include": "#autolink"
        },
        {
          "include": "#footnote_ref"
        },
        {
          "include": "#smart_typography"
        },
        {
          "include": "#escape"
        },
        {
          "include": "#hard_break"
        }
      ]
    },
    "escape": {
      "match": "\\\\[\\\\`*_{}\\[\\]()#+\\-.!~^/<>@%|=,]",
      "name": "constant.character.escape.carve"
    },
    "code_inline_multi": {
      "comment": "Two or more backticks: inline code that may itself contain single backticks; closed by the same run length (\\1).",
      "match": "(`{2,})(.+?)(\\1)",
      "captures": {
        "1": {
          "name": "punctuation.definition.raw.carve"
        },
        "2": {
          "name": "markup.raw.inline.carve"
        },
        "3": {
          "name": "punctuation.definition.raw.carve"
        }
      },
      "name": "markup.raw.inline.carve"
    },
    "code_inline_unpartnered": {
      "comment": "AN UNPARTNERED RUN IS A CODE SPAN TO THE END OF ITS PARAGRAPH, which is what the engine does with it and what Prism was taught in carve-grammars#312. Without this rule the run was not a code span at all here, so `a `*b*` coloured a strong inside a verbatim payload - 75 of 111 generated documents, and 37 of 73 on a wide run (carve-grammars#320). Tried AFTER the two closed-span rules, so a run that finds its partner on its own line is still the closed span; this one only sees the runs those cannot match. A BLANK LINE ENDS IT: the paragraph is where the engine stops the span, and a TextMate begin has no way to look ahead for a closer it already knows is not there. The opener is `+ rather than a single backtick so a wide unpartnered run is one opener rather than an opener plus payload.",
      "begin": "(`+)",
      "beginCaptures": {
        "1": {
          "name": "punctuation.definition.raw.carve"
        }
      },
      "end": "^(?=[ \\t]*$)",
      "name": "markup.raw.inline.carve",
      "contentName": "markup.raw.inline.carve"
    },
    "code_inline": {
      "match": "(`)([^`\\n]+)(`)",
      "captures": {
        "1": {
          "name": "punctuation.definition.raw.carve"
        },
        "2": {
          "name": "markup.raw.inline.carve"
        },
        "3": {
          "name": "punctuation.definition.raw.carve"
        }
      },
      "name": "markup.raw.inline.carve"
    },
    "image": {
      "comment": "BALANCED LABEL BODY. The spec closes a link label or an image alt text at the MATCHING `]` (grammar.ebnf `link_text`), and the scan is escape-aware. A `[^\\\\]]*` body closed at the FIRST `]`, so a nested bracket run left the whole construct unscoped - `![t[z]](/i.png)` is an image - and an UNBALANCED opener matched from the outer `[`, where the engine reads `[t` in `[t[z](/u)` as prose (carve-grammars#226). A regex cannot count, so the nesting is unrolled to four levels; deeper stays unscoped, which is what every depth did before.",
      "match": "(!\\[)((?:(?:[^\\[\\]\\\\]|\\\\[\\s\\S])|\\[(?:(?:[^\\[\\]\\\\]|\\\\[\\s\\S])|\\[(?:(?:[^\\[\\]\\\\]|\\\\[\\s\\S])|\\[(?:[^\\[\\]\\\\]|\\\\[\\s\\S])*\\])*\\])*\\])*)(\\])(\\()([^)\\s]+)(?:\\s+\"((?:[^\"\\\\]|\\\\.)*)\")?(\\))",
      "captures": {
        "1": {
          "name": "punctuation.definition.image.carve"
        },
        "2": {
          "name": "string.other.image.alt.carve"
        },
        "3": {
          "name": "punctuation.definition.image.carve"
        },
        "4": {
          "name": "punctuation.definition.link.carve"
        },
        "5": {
          "name": "markup.underline.link.image.carve"
        },
        "6": {
          "name": "string.other.link.title.carve"
        },
        "7": {
          "name": "punctuation.definition.link.carve"
        }
      }
    },
    "link_inline": {
      "comment": "BALANCED LABEL BODY. The spec closes a link label or an image alt text at the MATCHING `]` (grammar.ebnf `link_text`), and the scan is escape-aware. A `[^\\\\]]*` body closed at the FIRST `]`, so a nested bracket run left the whole construct unscoped - `![t[z]](/i.png)` is an image - and an UNBALANCED opener matched from the outer `[`, where the engine reads `[t` in `[t[z](/u)` as prose (carve-grammars#226). A regex cannot count, so the nesting is unrolled to four levels; deeper stays unscoped, which is what every depth did before.",
      "match": "(\\[)((?!\\])(?:(?:[^\\[\\]\\\\]|\\\\[\\s\\S])|\\[(?:(?:[^\\[\\]\\\\]|\\\\[\\s\\S])|\\[(?:(?:[^\\[\\]\\\\]|\\\\[\\s\\S])|\\[(?:[^\\[\\]\\\\]|\\\\[\\s\\S])*\\])*\\])*\\])*)(\\])(\\()([^)\\s]+)(?:\\s+\"((?:[^\"\\\\]|\\\\.)*)\")?(\\))",
      "captures": {
        "1": {
          "name": "punctuation.definition.link.carve"
        },
        "2": {
          "name": "string.other.link.title.carve"
        },
        "3": {
          "name": "punctuation.definition.link.carve"
        },
        "4": {
          "name": "punctuation.definition.link.carve"
        },
        "5": {
          "name": "markup.underline.link.carve"
        },
        "6": {
          "name": "string.other.link.description.carve"
        },
        "7": {
          "name": "punctuation.definition.link.carve"
        }
      }
    },
    "image_ref": {
      "comment": "THE OTHER TWO IMAGE SPELLINGS. The spec gives an image the same three forms a link has (grammar.ebnf `reference_image`, `collapsed_reference_image`) and says only the leading `!` and the `<img src>` output differ. This file had a rule for the inline form alone, so `![alt][ref]` and `![alt][]` fell through to `#link_ref`, which matched from the `[` and scoped them as a reference LINK with the `!` left as prose - the alt text carrying `string.other.link.title.carve`. That is worse than leaving them unscoped: a reader is told the document holds a link where it holds an image. Tried BEFORE `#link_ref` for that reason. The label body is the same four-level unroll `#link_ref` and `#image` carry, and the second bracket's star is what makes the collapsed form the same rule as the full one - the fold vim-carve and sublime-carve make for the link (carve-grammars#318). The alt text may be EMPTY, so it has no `(?!\\])` guard: `![][r]` is an image with no alt text, where `[][r]` is not a link.",
      "match": "(!\\[)((?:(?:[^\\[\\]\\\\]|\\\\[\\s\\S])|\\[(?:(?:[^\\[\\]\\\\]|\\\\[\\s\\S])|\\[(?:(?:[^\\[\\]\\\\]|\\\\[\\s\\S])|\\[(?:[^\\[\\]\\\\]|\\\\[\\s\\S])*\\])*\\])*\\])*)(\\])(\\[)([^\\]]*)(\\])",
      "captures": {
        "1": {
          "name": "punctuation.definition.image.carve"
        },
        "2": {
          "name": "string.other.image.alt.carve"
        },
        "3": {
          "name": "punctuation.definition.image.carve"
        },
        "4": {
          "name": "punctuation.definition.link.carve"
        },
        "5": {
          "name": "constant.other.reference.link.carve"
        },
        "6": {
          "name": "punctuation.definition.link.carve"
        }
      }
    },
    "link_ref": {
      "comment": "BALANCED LABEL BODY. The spec closes a link label or an image alt text at the MATCHING `]` (grammar.ebnf `link_text`), and the scan is escape-aware. A `[^\\\\]]*` body closed at the FIRST `]`, so a nested bracket run left the whole construct unscoped - `![t[z]](/i.png)` is an image - and an UNBALANCED opener matched from the outer `[`, where the engine reads `[t` in `[t[z](/u)` as prose (carve-grammars#226). A regex cannot count, so the nesting is unrolled to four levels; deeper stays unscoped, which is what every depth did before.",
      "match": "(\\[)((?!\\])(?:(?:[^\\[\\]\\\\]|\\\\[\\s\\S])|\\[(?:(?:[^\\[\\]\\\\]|\\\\[\\s\\S])|\\[(?:(?:[^\\[\\]\\\\]|\\\\[\\s\\S])|\\[(?:[^\\[\\]\\\\]|\\\\[\\s\\S])*\\])*\\])*\\])*)(\\])(\\[)([^\\]]*)(\\])",
      "captures": {
        "1": {
          "name": "punctuation.definition.link.carve"
        },
        "2": {
          "name": "string.other.link.title.carve"
        },
        "3": {
          "name": "punctuation.definition.link.carve"
        },
        "4": {
          "name": "punctuation.definition.link.carve"
        },
        "5": {
          "name": "constant.other.reference.link.carve"
        },
        "6": {
          "name": "punctuation.definition.link.carve"
        }
      }
    },
    "extension_inline": {
      "match": "(:)([a-zA-Z][\\w-]*)(\\[)([^\\]]*)(\\])(\\{[^}]*\\})?",
      "captures": {
        "1": {
          "name": "punctuation.definition.extension.carve"
        },
        "2": {
          "name": "entity.name.function.extension.carve"
        },
        "3": {
          "name": "punctuation.definition.extension.carve"
        },
        "4": {
          "name": "string.unquoted.extension.carve"
        },
        "5": {
          "name": "punctuation.definition.extension.carve"
        },
        "6": {
          "name": "meta.attributes.carve"
        }
      }
    },
    "symbol": {
      "match": "(?<!\\w)(:)([a-zA-Z0-9+-][\\w+-]*)(:)",
      "captures": {
        "1": {
          "name": "punctuation.definition.symbol.carve"
        },
        "2": {
          "name": "constant.language.symbol.carve"
        },
        "3": {
          "name": "punctuation.definition.symbol.carve"
        }
      }
    },
    "critic_add": {
      "match": "\\{\\+[^}]*\\+\\}",
      "name": "markup.inserted.carve"
    },
    "critic_del": {
      "match": "\\{-[^}]*-\\}",
      "name": "markup.deleted.carve"
    },
    "critic_sub": {
      "match": "\\{~[^}]*~>[^}]*~\\}",
      "name": "markup.changed.carve",
      "comment": "Editorial substitution requires the `~>` arrow; a `{~x~}` with no arrow is forced strikethrough (PART 9 S22)."
    },
    "critic_comment": {
      "comment": "THE PAYLOAD IS LITERAL AND MAY SPAN A LINE BREAK. The spec says nothing inside an editorial comment is parsed as markup and no escape is resolved. This was a single-line `match` whose body also excluded `}`, so `{# a } b #}` was not scoped at all and a comment across a break coloured the markup inside it - 87 of 286 generated documents. Same shape and same fix as `braced_comment`. A BLANK LINE ENDS IT, which is the bound Prism already uses for this construct - its pattern spells the body as a newline that is not followed by a blank line. A TextMate begin cannot look ahead for its own closer, because the regex sees one line, so without a bound an unclosed opener would scope the rest of the document: the failure the seven recorded unterminated runs in tests/lib/unterminated-fences.js describe. One paragraph is where the engine stops reading this construct too. AN ATTRIBUTE BLOCK OPENS THE SAME TWO CHARACTERS: `{#id .c key=value}` is an id plus classes, and a begin of `\\{#` alone claimed it and every line after it. The guard rejects the opener when a `}` NOT preceded by `#` closes on the same line, which is what separates an attribute block from a comment whose closer is `#}` - and leaves a multi-line comment, whose first line holds no `}` at all, opening as it should.",
      "begin": "\\{#(?![^}\\n]*(?<!#)\\})",
      "end": "#\\}|^(?=[ \\t]*$)",
      "name": "comment.block.critic.carve"
    },
    "forced_strong": {
      "comment": "Forced intraword bold (PART 9 S22). Content may contain the delimiter; the span ends at the closing `\\*}`.",
      "match": "(\\{\\*)(?=\\S)([^\\n]*?)(\\*\\})",
      "captures": {
        "1": {
          "name": "punctuation.definition.bold.carve"
        },
        "2": {
          "name": "markup.bold.carve"
        },
        "3": {
          "name": "punctuation.definition.bold.carve"
        }
      }
    },
    "forced_italic": {
      "comment": "Forced intraword italic (PART 9 S22). Content may contain the delimiter; the span ends at the closing `/}`.",
      "match": "(\\{/)(?=\\S)([^\\n]*?)(/\\})",
      "captures": {
        "1": {
          "name": "punctuation.definition.italic.carve"
        },
        "2": {
          "name": "markup.italic.carve"
        },
        "3": {
          "name": "punctuation.definition.italic.carve"
        }
      }
    },
    "forced_underline": {
      "comment": "Forced intraword underline (PART 9 S22). Content may contain the delimiter; the span ends at the closing `_}`.",
      "match": "(\\{_)(?=\\S)([^\\n]*?)(_\\})",
      "captures": {
        "1": {
          "name": "punctuation.definition.underline.carve"
        },
        "2": {
          "name": "markup.underline.carve"
        },
        "3": {
          "name": "punctuation.definition.underline.carve"
        }
      }
    },
    "forced_strike": {
      "comment": "Forced intraword strikethrough (PART 9 S22). Content may contain the delimiter; the span ends at the closing `~}`.",
      "match": "(\\{~)(?=\\S)([^\\n]*?)(~\\})",
      "captures": {
        "1": {
          "name": "punctuation.definition.strikethrough.carve"
        },
        "2": {
          "name": "markup.strikethrough.carve"
        },
        "3": {
          "name": "punctuation.definition.strikethrough.carve"
        }
      }
    },
    "attributes_empty": {
      "comment": "An empty attribute block is only valid glued to a preceding `]` (`[x]{}` -> <span>x</span>); a bare `{}` in prose is literal text (corpus 123).",
      "match": "(?<=\\])(\\{)(\\s*)(\\})",
      "captures": {
        "1": {
          "name": "punctuation.definition.attributes.carve"
        },
        "3": {
          "name": "punctuation.definition.attributes.carve"
        }
      }
    },
    "attributes_multiline": {
      "begin": "^(\\{)(?=[ \\t]*(?::(?:[A-Za-z0-9]{1,8}(?:-[A-Za-z0-9]{1,8})*)?|[.#][A-Za-z_][\\w-]*|[A-Za-z_][\\w-]*(?:=(?:\"(?:[^\"\\\\\\n]|\\\\.)*\"|'(?:[^'\\\\\\n]|\\\\.)*'|[^\\s\"'{}]+))?)(?:[ \\t]|$))",
      "beginCaptures": {
        "1": { "name": "punctuation.definition.attributes.carve" }
      },
      "end": "(\\})[ \\t]*$",
      "endCaptures": {
        "1": { "name": "punctuation.definition.attributes.carve" }
      },
      "name": "meta.attributes.carve",
      "comment": "A standalone attribute line may span physical lines. Anchoring the opener at column zero keeps an inline trailing attribute block line-local."
    },
    "attributes": {
      "match": "(\\{)(\\s*(?::(?:[A-Za-z0-9]{1,8}(?:-[A-Za-z0-9]{1,8})*)?|[.#][A-Za-z_][\\w-]*|[A-Za-z_][\\w-]*(?:=(?:\"(?:[^\"\\\\\\n]|\\\\.)*\"|'(?:[^'\\\\\\n]|\\\\.)*'|[^\\s\"'{}]+))?)(?:\\s+(?::(?:[A-Za-z0-9]{1,8}(?:-[A-Za-z0-9]{1,8})*)?|[.#][A-Za-z_][\\w-]*|[A-Za-z_][\\w-]*(?:=(?:\"(?:[^\"\\\\\\n]|\\\\.)*\"|'(?:[^'\\\\\\n]|\\\\.)*'|[^\\s\"'{}]+))?))*\\s*)(\\})",
      "captures": {
        "1": {
          "name": "punctuation.definition.attributes.carve"
        },
        "2": {
          "name": "meta.attributes.carve"
        },
        "3": {
          "name": "punctuation.definition.attributes.carve"
        }
      },
      "name": "meta.attributes.carve",
      "comment": "Strict identifier (spec PART 9 S14): a class/id/key may not start with a digit, so `{2=v}` stays literal text instead of scoping as an attribute block. An unquoted value may contain dots and colons."
    },
    "span": {
      "comment": "Balanced bracketed span text followed by a valid attribute block. Kept ahead of links so its text receives a stable span scope rather than only the trailing attributes being visible.",
      "match": "(\\[)((?!\\])(?:(?:[^\\[\\]\\\\]|\\\\[\\s\\S])|\\[(?:(?:[^\\[\\]\\\\]|\\\\[\\s\\S])|\\[(?:(?:[^\\[\\]\\\\]|\\\\[\\s\\S])|\\[(?:[^\\[\\]\\\\]|\\\\[\\s\\S])*\\])*\\])*\\])*)(\\])(\\{)(\\s*(?::(?:[A-Za-z0-9]{1,8}(?:-[A-Za-z0-9]{1,8})*)?|[.#][A-Za-z_][\\w-]*|[A-Za-z_][\\w-]*(?:=(?:\"(?:[^\"\\\\\\n]|\\\\.)*\"|'(?:[^'\\\\\\n]|\\\\.)*'|[^\\s\"'{}]+))?)(?:\\s+(?::(?:[A-Za-z0-9]{1,8}(?:-[A-Za-z0-9]{1,8})*)?|[.#][A-Za-z_][\\w-]*|[A-Za-z_][\\w-]*(?:=(?:\"(?:[^\"\\\\\\n]|\\\\.)*\"|'(?:[^'\\\\\\n]|\\\\.)*'|[^\\s\"'{}]+))?))*\\s*)(\\})",
      "captures": {
        "1": { "name": "punctuation.definition.span.carve" },
        "2": { "name": "string.other.link.title.carve" },
        "3": { "name": "punctuation.definition.span.carve" },
        "4": { "name": "punctuation.definition.attributes.carve" },
        "5": { "name": "meta.attributes.carve" },
        "6": { "name": "punctuation.definition.attributes.carve" }
      }
    },
    "bold_italic": {
      "match": "(/\\*)([^*]+)(\\*/)",
      "captures": {
        "1": {
          "name": "punctuation.definition.bold-italic.carve"
        },
        "2": {
          "name": "markup.bold.italic.carve"
        },
        "3": {
          "name": "punctuation.definition.bold-italic.carve"
        }
      }
    },
    "strong": {
      "match": "(?<![\\w*])(\\*)(?=\\S)([^*\\n]+?)(?<=\\S)(\\*)(?![\\w*])",
      "captures": {
        "1": {
          "name": "punctuation.definition.bold.carve"
        },
        "2": {
          "name": "markup.bold.carve"
        },
        "3": {
          "name": "punctuation.definition.bold.carve"
        }
      }
    },
    "italic": {
      "match": "(?<![\\w/])(/)(?=\\S)([^/\\n]+?)(?<=\\S)(/)(?![\\w/])",
      "captures": {
        "1": {
          "name": "punctuation.definition.italic.carve"
        },
        "2": {
          "name": "markup.italic.carve"
        },
        "3": {
          "name": "punctuation.definition.italic.carve"
        }
      }
    },
    "underline": {
      "match": "(?<![\\w_])(_)(?=\\S)([^_\\n]+?)(?<=\\S)(_)(?![\\w_])",
      "captures": {
        "1": {
          "name": "punctuation.definition.underline.carve"
        },
        "2": {
          "name": "markup.underline.text.carve"
        },
        "3": {
          "name": "punctuation.definition.underline.carve"
        }
      }
    },
    "strike": {
      "match": "(?<![\\w~])(~)(?=\\S)([^~\\n]+?)(?<=\\S)(~)(?![\\w~])",
      "captures": {
        "1": {
          "name": "punctuation.definition.strike.carve"
        },
        "2": {
          "name": "markup.strikethrough.carve"
        },
        "3": {
          "name": "punctuation.definition.strike.carve"
        }
      }
    },
    "highlight": {
      "comment": "A BARE `=` THAT BEGINS OR ENDS A SMART-TYPOGRAPHY PATTERN IS NOT A HIGHLIGHT OPENER (grammar.ebnf, Inline parsing precedence: \"a delimiter that begins a multi-char smart-typography pattern: `=>` is the arrow, never a highlight opener - the pattern is consumed first\"). Corpus 386's third paragraph is the shape it costs: `Not an arrow: key => value stays literal, and p <= q is a comparison.` renders with NO mark, and the opener `=` of `=>` reaching the closer `=` of `<=` scoped the whole 68-character sentence as a highlight (carve-grammars#325). That is worse than leaving a run uncoloured, because the output then claims the document holds a construct it does not - the same reason carve-grammars#324 gave for the arrows. THE GUARD IS ONLY WHAT THIS GRAMMAR MEASURABLY NEEDS. The opener may not be FOLLOWED by `>`, which is `=>` and the tail of `==>`; and it may not FOLLOW `!`, which is the `!=` comparison. It needs no guard against `<` or `>` behind it, and carried one until each character was reverted and measured: vscode-textmate resolves by POSITION, so on `a <=b c= d` the typography rule's `<=` starts one column before the highlight rule's `=` and takes it. `!=` is the comparison that rule does NOT carry (its alternation is arrows, dashes and `<=`/`>=`), which is why `!` is the one character here that changes a measurement. A leading `=` needs no guard either - the content class already refuses one. THE CLOSER IS DELIBERATELY NOT GUARDED, and the asymmetry is the engine's: once a highlight is OPEN the closer wins over the pattern, so `x =y z<= w` marks `y z<` and `x =y z=> w` marks `y z`. Measured against the pinned engine, both. ONE SHAPE IT COSTS: an autolink or a cross-reference glued straight to a highlight (`<https://e.example>=hi=`) really does mark, because there the `>` closes the link rather than opening a comparison - a flat token map cannot tell the two apart within a fixed-width lookbehind, and this takes the under-colouring side of that trade on the ticket's own reasoning.",
      "patterns": [
        {
          "match": "(\\{=)(?=\\S)([^\\n]*?)(=\\})",
          "captures": {
            "1": {
              "name": "punctuation.definition.highlight.carve"
            },
            "2": {
              "name": "markup.highlight.carve"
            },
            "3": {
              "name": "punctuation.definition.highlight.carve"
            }
          }
        },
        {
          "match": "(?<![=!\\w])(=)(?=\\S)(?!>)([^=\\n]+?)(?<=\\S)(=)(?![=\\w])",
          "captures": {
            "1": {
              "name": "punctuation.definition.highlight.carve"
            },
            "2": {
              "name": "markup.highlight.carve"
            },
            "3": {
              "name": "punctuation.definition.highlight.carve"
            }
          }
        }
      ]
    },
    "superscript": {
      "patterns": [
        {
          "match": "(\\{\\^)(?=\\S)([^\\n]*?)(\\^\\})",
          "captures": {
            "1": {
              "name": "punctuation.definition.superscript.carve"
            },
            "2": {
              "name": "markup.superscript.carve"
            },
            "3": {
              "name": "punctuation.definition.superscript.carve"
            }
          }
        }
      ]
    },
    "subscript": {
      "patterns": [
        {
          "match": "(\\{,)(?=\\S)([^,\\n]+?)(,\\})",
          "captures": {
            "1": {
              "name": "punctuation.definition.subscript.carve"
            },
            "2": {
              "name": "markup.subscript.carve"
            },
            "3": {
              "name": "punctuation.definition.subscript.carve"
            }
          }
        }
      ]
    },
    "mention": {
      "match": "(?<![\\w])(@)([a-zA-Z][\\w-]*)",
      "captures": {
        "1": {
          "name": "punctuation.definition.mention.carve"
        },
        "2": {
          "name": "variable.other.mention.carve"
        }
      }
    },
    "tag": {
      "match": "(?<![\\w])(#)([a-zA-Z][\\w-]*)",
      "captures": {
        "1": {
          "name": "punctuation.definition.tag.carve"
        },
        "2": {
          "name": "variable.other.tag.carve"
        }
      }
    },
    "heading_tag": {
      "comment": "Used only inside `heading` (carve-grammars#125), not the shared `#tag` include: a heading's own inline set does not carry `#cross_ref`, so without this guard a heading cross-reference's `#id` (`</#id>`, grammar.ebnf `auto_text_link`) would be wrongly claimed as a tag - corpus 118 pins `# A </#a>` staying unscoped as a whole. `#cross_ref` is not added here instead because that would mean modeling the full cross-reference construct (and its `>` closer) inside headings, well past this ticket's scope; excluding the one shape that collides is the narrower fix.",
      "match": "(?<![\\w])(?<!</)(#)([a-zA-Z][\\w-]*)",
      "captures": {
        "1": {
          "name": "punctuation.definition.tag.carve"
        },
        "2": {
          "name": "variable.other.tag.carve"
        }
      }
    },
    "autolink": {
      "match": "(<)([a-zA-Z][a-zA-Z0-9+.\\-]*:[^>\\s]+)(>)",
      "captures": {
        "1": {
          "name": "punctuation.definition.link.carve"
        },
        "2": {
          "name": "markup.underline.link.carve"
        },
        "3": {
          "name": "punctuation.definition.link.carve"
        }
      }
    },
    "footnote_ref": {
      "match": "(\\[)(\\^)([^\\]]+)(\\])",
      "captures": {
        "1": {
          "name": "punctuation.definition.footnote.carve"
        },
        "2": {
          "name": "punctuation.definition.footnote.carve"
        },
        "3": {
          "name": "constant.other.footnote.carve"
        },
        "4": {
          "name": "punctuation.definition.footnote.carve"
        }
      }
    },
    "smart_typography": {
      "match": "(<-->|<--|-->|<=>|<==|==>|---|--|\\.\\.\\.|<->|<-|->|<=|>=)",
      "name": "constant.other.smart_typography.carve"
    },
    "math_inline": {
      "match": "(\\$\\$?)(`)([^`\\n]*)(`)",
      "captures": {
        "1": {
          "name": "punctuation.definition.math.carve"
        },
        "2": {
          "name": "punctuation.definition.math.carve"
        },
        "3": {
          "name": "markup.math.carve"
        },
        "4": {
          "name": "punctuation.definition.math.carve"
        }
      }
    },
    "literal_inline_multi": {
      "comment": "A `!` prefix on two or more backticks: an inline literal that may itself contain single backticks; closed by the same run length (\\2).",
      "match": "(!)(`{2,})(.+?)(\\2)",
      "captures": {
        "1": {
          "name": "punctuation.definition.literal.carve"
        },
        "2": {
          "name": "punctuation.definition.raw.carve"
        },
        "3": {
          "name": "markup.raw.inline.content.carve"
        },
        "4": {
          "name": "punctuation.definition.raw.carve"
        }
      }
    },
    "literal_inline": {
      "match": "(!)(`)([^`\\n]*)(`)",
      "captures": {
        "1": {
          "name": "punctuation.definition.literal.carve"
        },
        "2": {
          "name": "punctuation.definition.raw.carve"
        },
        "3": {
          "name": "markup.raw.inline.content.carve"
        },
        "4": {
          "name": "punctuation.definition.raw.carve"
        }
      }
    },
    "footnote_inline": {
      "match": "(\\^\\[)([^\\]\\n]*)(\\])",
      "captures": {
        "1": {
          "name": "punctuation.definition.footnote.carve"
        },
        "2": {
          "name": "string.other.footnote.inline.carve"
        },
        "3": {
          "name": "punctuation.definition.footnote.carve"
        }
      }
    },
    "cross_ref": {
      "match": "(</#)([^>\\s]+)(>)",
      "captures": {
        "1": {
          "name": "punctuation.definition.link.carve"
        },
        "2": {
          "name": "markup.underline.link.cross-reference.carve"
        },
        "3": {
          "name": "punctuation.definition.link.carve"
        }
      }
    },
    "hard_break": {
      "match": "(\\\\)$",
      "captures": {
        "1": {
          "name": "constant.character.escape.hard-break.carve"
        }
      }
    },
    "definition_list": {
      "comment": "A `:` description line scopes only INSIDE an entry a real `:: ` term opened (carve-grammars#91) - a bare `:` line at the top level (no preceding term, or a term the separator rule disqualified, e.g. `::\\tterm`) is prose and carries no scope. `begin` opens on a real term line (separator is a literal space per the tab-and-separator ruling on markup-carve/carve#698 - a tab never satisfies it); `end` closes on the next OTHER block opener (heading, list, quote, fence, hr, caption, table row), tolerating blank lines and lazy-continuation prose in between so a folded term (`term_continuation_line`) or a definition separated from its term by one blank line (corpus 25-definition-lists-7) still scope.",
      "patterns": [
        {
          "begin": "^(?:\\A\\x{FEFF})?([ \\t]*)(::)( +)(?![ \\t]*$)(.*)$",
          "beginCaptures": {
            "2": {
              "name": "punctuation.definition.list.definition.carve"
            },
            "4": {
              "name": "entity.name.tag.definition.term.carve"
            }
          },
          "end": "^[ \\t]*(?:#{1,6} |-{3,}[ \\t]*$|\\*{3,}[ \\t]*$|_{3,}[ \\t]*$|`{3,}|~{3,}|:{3,}|>(?: |$)|\\^ |\\||[-*][ \\t]|[-*]\\{|\\d+[.)][ \\t]|[A-Za-z]+[.)][ \\t]|\\.[ \\t])",
          "patterns": [
            {
              "match": "^([ \\t]*)(::)( +)(?![ \\t]*$)(.*)$",
              "captures": {
                "2": {
                  "name": "punctuation.definition.list.definition.carve"
                },
                "4": {
                  "name": "entity.name.tag.definition.term.carve"
                }
              }
            },
            {
              "match": "^([ \\t]*)(:)(?=[ ])",
              "captures": {
                "2": {
                  "name": "punctuation.definition.list.definition.carve"
                }
              }
            }
          ]
        }
      ]
    },
    "citation": {
      "comment": "Citation group [@key]; ported from intellij-carve. Citation group: a bracket whose content contains at least one @key and has no (url)/[ref]/{attrs} tail. A leading + signals an integral/in-text citation. Items are ;-separated; each may carry a -/+ prefix and an optional , locator. The @key and the -/+ per-item markers are scoped distinctly, analogous to @mention but inside the citation bracket.",
      "patterns": [
        {
          "name": "markup.citation.carve",
          "comment": "Match the whole citation bracket [+?...@key...] that is NOT followed by (, [, or { (which would make it a link, ref-link, or span). A lookahead for @ inside the brackets discriminates from plain spans and footnote refs.",
          "begin": "(?<!!)(?<![\\\\])(\\[)(\\+)?(?=[^\\]\\n]*@[A-Za-z0-9_][^\\]\\n]*\\](?!\\(|\\[|\\{))",
          "beginCaptures": {
            "1": {
              "name": "punctuation.definition.citation.begin.carve"
            },
            "2": {
              "name": "keyword.operator.citation.integral.carve"
            }
          },
          "end": "(\\])(?!\\(|\\[|\\{)",
          "endCaptures": {
            "1": {
              "name": "punctuation.definition.citation.end.carve"
            }
          },
          "patterns": [
            {
              "comment": "Optional -/+ per-item modifier immediately before the @key",
              "name": "keyword.operator.citation.modifier.carve",
              "match": "(?<=^|;|\\[|\\+)\\s*(-)"
            },
            {
              "comment": "@key: citation key with allowed charset [A-Za-z0-9_][A-Za-z0-9_:.#$%&+?<>~/-]*",
              "name": "variable.other.citation.key.carve",
              "match": "@[A-Za-z0-9_][A-Za-z0-9_:.#$%&+?<>~\\/-]*"
            },
            {
              "comment": "Semicolon item separator",
              "name": "punctuation.separator.citation.carve",
              "match": ";"
            }
          ]
        }
      ]
    },
    "callout_annotation": {
      "comment": "Leading callout annotation line: <N> at the very start (with optional leading whitespace), followed by a space and annotation prose.",
      "name": "markup.callout.annotation.carve",
      "match": "^(?:\\A\\x{FEFF})?(\\s*)(<)([0-9]+)(>)(?=\\s)",
      "captures": {
        "2": {
          "name": "punctuation.definition.callout.carve"
        },
        "3": {
          "name": "constant.numeric.callout.carve"
        },
        "4": {
          "name": "punctuation.definition.callout.carve"
        }
      }
    },
    "callout_marker": {
      "comment": "Trailing callout marker inside a code line: <N> at the end of the line (optionally preceded by whitespace).",
      "name": "markup.callout.marker.carve",
      "match": "(<)([0-9]+)(>)\\s*$",
      "captures": {
        "1": {
          "name": "punctuation.definition.callout.carve"
        },
        "2": {
          "name": "constant.numeric.callout.carve"
        },
        "3": {
          "name": "punctuation.definition.callout.carve"
        }
      }
    },
    "block_comment_on_quote_marker_line": {
      "comment": "A COMMENT FENCE MAY OPEN ON A BLOCK-QUOTE MARKER LINE (`> %%%`) too, and the same rule hides its body: spec PART 9 section 24 S2 with section 28 make a comment's body verbatim and invisible WHEREVER the fence sits (corpus 70-blocks-that-render-to-nothing - `> q` / `> %%%` / `> x` / `> %%%` / `> body` renders the quote with `q` and `body` only, carve-grammars#245). `#block_comment` cannot reach it: its begin allows only whitespace before the run, so all three lines fell through to `#blockquote` and scoped as plain quote with nothing marked hidden. This rule leads `#blockquote` at every site that includes it, and the marker run is capture 1 so it keeps its quote scope - tree-sitter-carve splits it the same way, with the `fenced_comment_block` inside the quote's `content` beside the `block_quote_marker`. The end has TWO branches. The first is the closer, which repeats the opener's MARKER RUN as well as its fence width, so a fence opened at `> > ` is not closed at `> `. The second is a zero-width quote boundary: an UNMARKED line is where the quote can end, and the engine degrades an unclosed opener to a line comment rather than hiding anything (`> %%%` / `> c` / blank line leaves `c` VISIBLE). Unlike the Prism and highlight.js grammars this one cannot demand the closer up front - a TextMate begin sees one line - so an unclosed opener still hides the rest of its own quote here; the boundary branch is what keeps that from reaching the document below it. Anchored at COLUMN 0 for the same reason `#blockquote` is (carve-grammars#138): an indented marker at the top level is literal text, `  > %%%` renders `<p>&gt; ...</p>` against the pinned engine, so `#block_comment_on_quote_marker_line_in_container` takes over inside a container.",
      "begin": "^(?:\\A\\x{FEFF})?((?:> )+)(%{3,})(?!%)[^\\n]*$",
      "beginCaptures": {
        "1": {
          "name": "punctuation.definition.quote.carve"
        }
      },
      "end": "^\\1\\2(?!%)[^\\n]*$|^(?!>)",
      "name": "comment.block.carve"
    },
    "block_comment_on_quote_marker_line_in_container": {
      "comment": "The in-container form of `#block_comment_on_quote_marker_line` - see there for what it pins and why. Leading whitespace is the container's own indent, so it is allowed here and only here, exactly as `#blockquote_in_container` splits from `#blockquote`. Reached from `#container_blocks`, whose `end` has already established that an indented line inside the container sits at or beyond the item's content column.",
      "begin": "^(?:\\A\\x{FEFF})?[ \\t]*((?:> )+)(%{3,})(?!%)[^\\n]*$",
      "beginCaptures": {
        "1": {
          "name": "punctuation.definition.quote.carve"
        }
      },
      "end": "^[ \\t]*\\1\\2(?!%)[^\\n]*$|^(?![ \\t]*>)",
      "name": "comment.block.carve"
    },
    "block_comment_on_quote_on_marker_line": {
      "comment": "A COMMENT FENCE MAY OPEN ON A QUOTE THAT ITSELF OPENED ON A LIST ITEM'S MARKER LINE (`- > %%%`). carve-grammars#246 left this shape out on the ground that a shape one grammar handles and two do not is drift, and Prism and highlight.js reach it now, so the reason has inverted (carve-grammars#259). Anchored on \\\\G rather than ^, exactly as `#block_comment_on_marker_line` is and for the same reason: \\\\G matches at the current scan position, which on a marker line is where the marker ended, and vscode-textmate resets it on every following line so this can never open one. The `(?<=[ \\\\t])` guard keeps it to a marker line specifically - a later match on the same line moves the \\\\G anchor too. It LEADS `#blockquote_on_marker_line` in `#container_blocks`, which would otherwise take the same position and leave the fence to `#trailing_comment` as a LINE comment, with the hidden body coming back as live syntax. The end is `#block_comment_on_quote_marker_line`'s, unchanged: the closer repeats the opener's marker run and fence width, and the second branch is a zero-width quote boundary so an unclosed opener cannot reach the document below.",
      "begin": "\\G(?<=[ \\t])((?:> )+)(%{3,})(?!%)[^\\n]*$",
      "beginCaptures": {
        "1": {
          "name": "punctuation.definition.quote.carve"
        }
      },
      "end": "^[ \\t]*\\1\\2(?!%)[^\\n]*$|^(?![ \\t]*>)",
      "name": "comment.block.carve"
    },
    "blockquote_on_marker_line": {
      "comment": "A BLOCK QUOTE MAY OPEN ON A LIST ITEM'S OWN MARKER LINE (`- > x`, `1. > x`, `- - > x`, `- [ ] > x`), and it takes the rest of that line: carve-js renders `<ul><li><blockquote><p>x</p></blockquote></li></ul>`. Neither `#blockquote` nor `#blockquote_in_container` can reach it - both are anchored on ^, and the container's own begin match has already consumed the marker by the time this line is scanned - so the whole line fell through to the list rule and everything after the marker carried no quote scope at all (carve-grammars#259). Anchored on \\\\G with the same `(?<=[ \\\\t])` guard `#block_comment_on_marker_line` uses, for the reasons stated there. Reachable ONLY from `#container_blocks`, so a document-level line can never take it. The marker run is capture 1 and keeps its quote punctuation scope, which is tree-sitter-carve's split: the `block_quote` sits inside `list_item_content` beside the `list_marker_*` rather than over it.",
      "match": "\\G(?<=[ \\t])(>)(?= |$) ?(.*)$",
      "captures": {
        "1": {
          "name": "punctuation.definition.quote.carve"
        },
        "2": {
          "name": "markup.quote.carve"
        }
      },
      "name": "markup.quote.carve"
    },
    "block_comment_on_marker_line": {
      "comment": "A COMMENT FENCE MAY OPEN ON A LIST ITEM'S MARKER LINE (`- %%%`), and then its body is hidden exactly as it is anywhere else: spec PART 9 section 24 S2 with section 28 make a comment's body verbatim and invisible WHEREVER the fence sits, so the whole run from opener to closer is one comment and nothing after it is affected (corpus 337-a-comment-fence-opened-on-an-item-s-marker-line-hides-its-body-too, carve-grammars#243). `#block_comment` cannot reach that shape: its begin is anchored on ^, and the container's own begin match has already consumed `- ` by the time this line is scanned. So the `%%%` fell through to `#trailing_comment` and scoped as a LINE comment, no block opened, the hidden body came back as live syntax, and the real closer was then taken for an opener. Anchored on \\G rather than ^: \\G matches at the current scan position, which on a marker line is exactly where the marker ended, and vscode-textmate resets it on every following line so this can never open one. The `(?<=[ \\t])` guard is what keeps it to a marker line specifically: a later match on the same line moves the \\G anchor too, and a percent run glued to the end of one (`- /a/%%%`) is not a fence. The end has TWO branches. The first is the ordinary closer, INDENTED because it sits at the item's content column. The second is a zero-width container boundary: a column-0 line ends the item and with it the open fence, so corpus 326-a-column-0-line-after-a-container-s-last-block-when-that-block-left-no-paragraph-open-6 (`- %%%` / `c` / `%%%` / `tail`) keeps `c` and `tail` VISIBLE. Without it an unclosed opener would run to end of document.",
      "begin": "\\G(?<=[ \\t])(%{3,})(?!%)[^\\n]*$",
      "end": "^[ \\t]+\\1(?!%)[^\\n]*$|^(?=[^ \\t\\n])",
      "name": "comment.block.carve"
    },
    "container_blocks": {
      "comment": "The block pattern set a container's body reaches, shared by every list-item rule so the three of them cannot drift apart. It is the top-level list minus `#frontmatter`, which is document-start only. `#link_ref_def_in_container` stands in for `#link_ref_def`: a definition is flush-left at document level and indented here, and the container's `end` has already established that an indented line inside it sits at or beyond the item's content column. `#block_comment_on_marker_line` leads, and is reachable ONLY from here: it is the one rule that has to fire mid-line, on the container's own opening line.",
      "patterns": [
        {
          "include": "#block_comment_on_marker_line"
        },
        {
          "include": "#block_comment_on_quote_on_marker_line"
        },
        {
          "include": "#blockquote_on_marker_line"
        },
        {
          "include": "#fenced_code_in_container"
        },
        {
          "include": "#heading_in_container"
        },
        {
          "include": "#hr"
        },
        {
          "include": "#block_comment"
        },
        {
          "include": "#line_comment"
        },
        {
          "include": "#figure_group"
        },
        {
          "include": "#admonition"
        },
        {
          "include": "#block_comment_on_quote_marker_line_in_container"
        },
        {
          "include": "#blockquote_in_container"
        },
        {
          "include": "#abbreviation_in_container"
        },
        {
          "include": "#link_ref_def_in_container"
        },
        {
          "include": "#caption"
        },
        {
          "include": "#definition_list"
        },
        {
          "include": "#table_row"
        },
        {
          "include": "#list_continuation"
        },
        {
          "include": "#multiline_cell"
        },
        {
          "include": "#callout_annotation"
        },
        {
          "include": "#task_item"
        },
        {
          "include": "#list_item"
        },
        {
          "include": "#numbered_item"
        },
        {
          "include": "#inline"
        }
      ]
    },
    "footnote_def": {
      "comment": "A footnote definition is a container too, and it is the reason `#link_ref_def` could be narrowed to flush-left at all. Its body's content column is a FIXED two, whatever the label is wide: measured against carve-php, `[^a]: note` followed by `  [r]: /u` collects the definition (spec test 202-a-definition-on-a-footnote-body-s-continuation-line-is-collected), one column in leaves it a top-level paragraph, and three or more folds it into the note's own text. Without this rule that corpus-pinned shape lost its scope when definitions stopped matching at any indent - the container is what gives it back, on the same mechanism the list rules use. FLUSH-LEFT, like `#link_ref_def` beside it: an indented footnote definition at document level is a paragraph (spec 159), and inside a container the enclosing list item is already open and carries its own body patterns. The opener otherwise reproduces `#footnote_ref`'s scopes so the definition line itself tokenizes as before. The separator must be a literal space (markup-carve/carve#698 - a tab does not separate), so `[^a]:\\tnote` opens nothing and stays the paragraph it renders as.",
      "begin": "^(?:\\A\\x{FEFF})?(\\[)(\\^)([^\\]]+)(\\])(:)(?= )",
      "beginCaptures": {
        "1": {
          "name": "punctuation.definition.footnote.carve"
        },
        "2": {
          "name": "punctuation.definition.footnote.carve"
        },
        "3": {
          "name": "constant.other.footnote.carve"
        },
        "4": {
          "name": "punctuation.definition.footnote.carve"
        },
        "5": {
          "name": "punctuation.separator.carve"
        }
      },
      "end": "^(?![ \\t]*$)(?![ \\t]{2})",
      "patterns": [
        {
          "include": "#container_blocks"
        }
      ]
    },
    "heading_in_container": {
      "begin": "^[ \\t]*(#{1,6}) +(?![ \\t]*$)",
      "beginCaptures": {
        "1": {
          "name": "punctuation.definition.heading.carve"
        }
      },
      "end": "$",
      "name": "markup.heading.carve",
      "contentName": "entity.name.section.carve",
      "patterns": [
        {
          "include": "#heading_tag"
        }
      ],
      "comment": "The in-container heading: leading whitespace is the container's own indent, so it is allowed here and only here. `#container_blocks` reaches this; the document-level `#heading` stays anchored at column 0 (carve-grammars#138)."
    }
  }
}
